version: '3.8'

services:
  qdrant:
    image: qdrant/qdrant:latest
    container_name: medical-qdrant
    ports:
      - "6333:6333"  # HTTP API port
      - "6334:6334"  # gRPC API port
    volumes:
      - qdrant_storage:/qdrant/storage:rw
    restart: always

volumes:
  qdrant_storage:
